{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for f-core host.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
